

用 curl 的方式的文档是否合理？是否应该增加更多的现成的脚本，而不只是说明书？


用dingtalk的sdk，能够自动的升级最新的，目前的文档是写死的情况，无法自动升级。或许skill中引导安装最新的，同时提供升级的提示词会更好，此外，通过sdk代码量实际上很低，也节约了大模型的token，因为是封装后的抽象，此外也更加的稳定调用上。

最重要的是成功率将会更高，但是这个需要具体的验证。

如果采用这样的方案，那么需要写若干 scripts/xxxxx.py 脚本，不过其实也还好。


其实，还有一个相当大的问题，api.md 文档这种东西，里面包含的代码相关的说明，实际上是无法验证正确与否的，如果大模型幻觉了，那么就出问题了。而使用脚本的方式，是可以验证其正确性质的，所以没有任何的问题。